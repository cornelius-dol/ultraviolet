const UvVersion = Object.freeze((() => {
    let VERSION = "1.01"; /*2026.0630.2202*/                                                                            // version (the commented datestamp is when the version was last updated)
    let BUILD   = "2026.0630.2202";                                                                                     // build datestamp (separate from the version datestamp so they can be updated independently)
    return { VERSION, BUILD };
    })());
