const UvVersion = Object.freeze((() => {
    let VERSION = "1.00"; /*2026.0623.2259*/                                                                            // version (the commented datestamp is when the version was last updated)
    let BUILD   = "2026.0623.2259";                                                                                     // build datestamp (separate from the version datestamp so they can be updated independently)
    return { VERSION, BUILD };
    })());
